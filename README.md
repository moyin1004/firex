# firex
Self Tool

测试环境：
- Ubuntu 24.04 LTS
- gcc 13.3.0

## 安装依赖
```bash
# drogon 依赖
sudo apt install libjsoncpp-dev uuid-dev zlib1g-dev
sudo apt install openssl libssl-dev # ssl可选
sudo apt-get install libsqlite3-dev # sqlite3可选
```